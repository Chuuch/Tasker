apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-data
  namespace: {{ .Release.Namespace }}

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: {{ .Values.database.storage.size }}
